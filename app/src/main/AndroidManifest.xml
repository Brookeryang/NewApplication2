<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yyh.newapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <!--<intent-filter>-->
            <!--<action android:name="android.intent.action.MAIN" />-->

            <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        </activity>
        <activity
            android:name=".DetailsActivity"
            android:theme="@style/DetailsActivity.NoActionBar" />
        <activity android:name=".SettingTextViewActivity" />
        <!-- Android 高仿 IOS 滚轮选择控件 -->
        <activity android:name=".DialogActivity" />
        <!-- 刷新Activity -->
        <activity android:name=".RefreshActivity" />
        <!-- 获取验证码 -->
        <activity android:name=".AcquiringVerificationCodeActivity" />
        <!-- 好看的登陆 -->
        <activity android:name=".login.LoginActivity" />
        <!-- 请求类 -->
        <activity android:name=".http.HttpContentActivity" />
        <activity android:name=".http.DataActivity_02" />
        <!-- 定制你的输入法软键盘右下角|Tips -->
        <activity android:name=".SearchActivity" />
        <!-- 自定义的Dialog -->
        <activity android:name=".My_DialogActivity" />
        <!-- 沉浸式状态栏+ScrollView顶部伸缩+ActionBar渐变 -->
        <activity
            android:name=".ScrollViewActionBarActivity"
            android:theme="@style/TranslucentTheme" />
        <!-- RxBus测试 -->
        <activity android:name=".RxBusActivity" />
        <!-- 第二个RxBus的页面 -->
        <activity android:name=".rxbus.SecondActivity" />
        <!--Android 5.0新特性Test-->
        <activity android:name=".materil_design.NewAndroidActivity_5">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>